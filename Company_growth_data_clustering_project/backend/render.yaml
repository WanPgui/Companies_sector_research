services:
  - type: web
    name: Companies_sector_research
    env: python
    plan: free
    buildCommand: |
      cd backend
      pip install -r requirements.txt
    startCommand: |
      cd backend
      gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: "10000"
